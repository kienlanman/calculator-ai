# Máy tính đơn giản

Chương trình `calculator.py` cung cấp 4 phép tính cơ bản (cộng, trừ, nhân, chia) trên dòng lệnh.

## Cách chạy

```bash
python3 calculator.py
```

Sau khi chạy, chọn phép tính mong muốn và nhập hai số. Nhấn `q` để thoát ứng dụng.